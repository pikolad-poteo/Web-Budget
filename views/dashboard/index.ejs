<%- include('../partials/header', { user, activePage: 'dashboard' }) %>

<div class="row mb-3">
  <div class="col-12">
    <h2>Дашборд</h2>
    <p class="text-muted">
      Привет, <%= user.name || user.email %>!
    </p>
  </div>
</div>

<div class="row section-card">
  <div class="col-12">
    <%- include('_summary_cards', { balance, income, expense }) %>
  </div>
</div>

<div class="row section-card">
  <div class="col-12">
    <%- include('_categories_expenses', { actualCategories, plannedCategories, totalExpensesAbs }) %>
  </div>
</div>

<div class="row section-card">
  <div class="col-12">
    <%- include('_actions') %>
  </div>
</div>

<%- include('../partials/footer') %>
